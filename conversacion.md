# Conversación del proyecto

---

**Agente:** He separado el `preview.html` en tres archivos: `styles.css`, `index.html` y `app.js`. Añadí dos pestañas nuevas (Proyectos y Estadísticas) y el botón para guardar todo en JSON. Sobre Supabase: sí se puede conectar solo con HTML/CSS/JS; la respuesta detallada está en `README.md` en la sección "Conexión con Supabase".
